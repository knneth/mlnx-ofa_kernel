From: Valentine Fatiev <valentinef@nvidia.com>
Subject: [PATCH] BACKPORT: drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c

---
 .../ethernet/mellanox/mlx5/core/en/tc_tun.c   | 49 +++++++++++++++++--
 1 file changed, 44 insertions(+), 5 deletions(-)

diff --git a/drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c b/drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c
index xxxxxxx..xxxxxxx 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c
@@ -2,7 +2,12 @@
 /* Copyright (c) 2018 Mellanox Technologies. */
 
 #include <net/flow.h>
+#ifdef HAVE_NET_INET_DSCP_H
 #include <net/inet_dscp.h>
+#endif
+#ifndef HAVE_NET_IPV6_STUBS_H
+#include <net/ip6_route.h>
+#endif
 #include <net/vxlan.h>
 #include <net/gre.h>
 #include <net/geneve.h>
@@ -44,8 +49,10 @@ struct mlx5e_tc_tunnel *mlx5e_get_tc_tun(struct net_device *tunnel_dev)
 	else if (netif_is_gretap(tunnel_dev) ||
 		 netif_is_ip6gretap(tunnel_dev))
 		return &gre_tunnel;
+#if defined(HAVE_NET_BAREUDP_H) && defined(HAVE_FLOW_DISSECTOR_MPLS_LSE)
 	else if (netif_is_bareudp(tunnel_dev))
 		return &mplsoudp_tunnel;
+#endif
 	else
 		return NULL;
 }
@@ -136,7 +143,11 @@ static int mlx5e_route_lookup_ipv4_get(struct mlx5e_priv *priv,
 		goto err_rt_release;
 	}
 
-	if (mlx5_lag_is_multipath(mdev) && rt->rt_gw_family != AF_INET) {
+#ifdef HAVE_RT_GW_FAMILY
+       if (mlx5_lag_is_multipath(mdev) && rt->rt_gw_family != AF_INET) {
+#else
+	if (mlx5_lag_is_multipath(mdev) && !rt->rt_uses_gateway) {
+#endif
 		ret = -ENETUNREACH;
 		goto err_rt_release;
 	}
@@ -234,7 +245,13 @@ int mlx5e_tc_tun_create_header_ipv4(struct mlx5e_priv *priv,
 	int err;
 
 	/* add the IP fields */
+#ifdef HAVE_FLOWI4_DSCP
+/* Newer kernels (6.18+): flowi4_dscp field with dscp_t type */
 	attr.fl.fl4.flowi4_dscp = inet_dsfield_to_dscp(tun_key->tos);
+#else
+/* Older kernels: flowi4_tos field with __u8 type */
+	attr.fl.fl4.flowi4_tos = tun_key->tos & ~INET_ECN_MASK;
+#endif
 	attr.fl.fl4.daddr = tun_key->u.ipv4.dst;
 	attr.fl.fl4.saddr = tun_key->u.ipv4.src;
 	attr.ttl = tun_key->ttl;
@@ -350,7 +367,13 @@ int mlx5e_tc_tun_update_header_ipv4(struct mlx5e_priv *priv,
 	int err;
 
 	/* add the IP fields */
+#ifdef HAVE_FLOWI4_DSCP
+/* Newer kernels (6.18+): flowi4_dscp field with dscp_t type */
 	attr.fl.fl4.flowi4_dscp = inet_dsfield_to_dscp(tun_key->tos);
+#else
+/* Older kernels: flowi4_tos field with __u8 type */
+	attr.fl.fl4.flowi4_tos = tun_key->tos & ~INET_ECN_MASK;
+#endif
 	attr.fl.fl4.daddr = tun_key->u.ipv4.dst;
 	attr.fl.fl4.saddr = tun_key->u.ipv4.src;
 	attr.ttl = tun_key->ttl;
@@ -439,24 +462,34 @@ release_neigh:
 	return err;
 }
 
-#if IS_ENABLED(CONFIG_INET) && IS_ENABLED(CONFIG_IPV6)
 static int mlx5e_route_lookup_ipv6_get(struct mlx5e_priv *priv,
 				       struct net_device *dev,
 				       struct mlx5e_tc_tun_route_attr *attr)
 {
 	struct mlx5e_tc_tunnel *tunnel = mlx5e_get_tc_tun(dev);
 	struct net_device *route_dev;
+	struct dst_entry *dst = NULL;
 	struct net_device *out_dev;
-	struct dst_entry *dst;
 	struct neighbour *n;
+#if IS_ENABLED(CONFIG_INET) && IS_ENABLED(CONFIG_IPV6)
 	int ret;
 
 	if (tunnel && tunnel->get_remote_ifindex)
 		attr->fl.fl6.flowi6_oif = tunnel->get_remote_ifindex(dev);
+#ifndef HAVE_NET_IPV6_STUBS_H
+/* Newer kernels (7.1+): ipv6_stub removed, use direct call */
+	dst = ip6_dst_lookup_flow(dev_net(dev), NULL, &attr->fl.fl6, NULL);
+#else
+/* Older kernels: use ipv6_stub indirection */
 	dst = ipv6_stub->ipv6_dst_lookup_flow(dev_net(dev), NULL, &attr->fl.fl6,
 					      NULL);
+#endif
 	if (IS_ERR(dst))
 		return PTR_ERR(dst);
+#ifdef HAVE_BASECODE_EXTRAS
+	if (!dst)
+		return -EOPNOTSUPP;
+#endif
 
 	if (!attr->ttl)
 		attr->ttl = ip6_dst_hoplimit(dst);
@@ -464,6 +497,9 @@ static int mlx5e_route_lookup_ipv6_get(struct mlx5e_priv *priv,
 	ret = get_route_and_out_devs(priv, dst->dev, &route_dev, &out_dev);
 	if (ret < 0)
 		goto err_dst_release;
+#else
+	return -EOPNOTSUPP;
+#endif
 
 	dev_hold(route_dev);
 	n = dst_neigh_lookup(dst, &attr->fl.fl6.daddr);
@@ -707,7 +743,6 @@ release_neigh:
 	mlx5e_route_lookup_ipv6_put(&attr);
 	return err;
 }
-#endif
 
 int mlx5e_tc_tun_route_lookup(struct mlx5e_priv *priv,
 			      struct mlx5_flow_spec *spec,
@@ -809,7 +844,7 @@ int mlx5e_tc_tun_parse(struct net_device *filter_dev,
 				       outer_headers);
 	struct netlink_ext_ack *extack = f->common.extack;
 	int err = 0;
-
+	
 	if (!tunnel) {
 		netdev_warn(priv->netdev,
 			    "decapsulation offload is not supported for %s net device\n",
@@ -823,6 +858,7 @@ int mlx5e_tc_tun_parse(struct net_device *filter_dev,
 	if (tunnel->parse_udp_ports) {
 		err = tunnel->parse_udp_ports(priv, spec, f,
 					      headers_c, headers_v);
+
 		if (err)
 			goto out;
 	}
@@ -830,6 +866,7 @@ int mlx5e_tc_tun_parse(struct net_device *filter_dev,
 	if (tunnel->parse_tunnel) {
 		err = tunnel->parse_tunnel(priv, spec, f,
 					   headers_c, headers_v);
+
 		if (err)
 			goto out;
 	}
@@ -943,6 +980,7 @@ int mlx5e_tc_tun_parse_udp_ports(struct mlx5e_priv *priv,
 				 struct flow_cls_offload *f,
 				 void *headers_c,
 				 void *headers_v)
+
 {
 	struct flow_rule *rule = flow_cls_offload_flow_rule(f);
 	struct netlink_ext_ack *extack = f->common.extack;
@@ -991,3 +1029,4 @@ int mlx5e_tc_tun_parse_udp_ports(struct mlx5e_priv *priv,
 
 	return 0;
 }
+
