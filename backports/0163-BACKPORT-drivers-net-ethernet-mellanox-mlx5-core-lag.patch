From: Valentine Fatiev <valentinef@nvidia.com>
Subject: [PATCH] BACKPORT: drivers/net/ethernet/mellanox/mlx5/core/lag/lag.c

---
 .../net/ethernet/mellanox/mlx5/core/lag/lag.c | 101 ++++++++++++++++++
 1 file changed, 101 insertions(+)

diff --git a/drivers/net/ethernet/mellanox/mlx5/core/lag/lag.c b/drivers/net/ethernet/mellanox/mlx5/core/lag/lag.c
index xxxxxxx..xxxxxxx 100644
--- a/drivers/net/ethernet/mellanox/mlx5/core/lag/lag.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/lag/lag.c
@@ -51,6 +51,7 @@
  */
 static DEFINE_SPINLOCK(lag_lock);
 
+
 static int get_port_sel_mode(enum mlx5_lag_mode mode, unsigned long flags)
 {
 	if (test_bit(MLX5_LAG_MODE_FLAG_HASH_BASED, &flags))
@@ -175,6 +176,9 @@ static void mlx5_infer_tx_disabled(struct lag_tracker *tracker, struct mlx5_lag
 				   u8 *ports, int *num_disabled)
 {
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	*num_disabled = 0;
 	mlx5_ldev_for_each(i, 0, ldev)
@@ -187,6 +191,9 @@ void mlx5_infer_tx_enabled(struct lag_tracker *tracker, struct mlx5_lag *ldev,
 			   u8 *ports, int *num_enabled)
 {
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	*num_enabled = 0;
 	mlx5_ldev_for_each(i, 0, ldev)
@@ -224,6 +231,9 @@ static void mlx5_lag_print_mapping(struct mlx5_core_dev *dev,
 		buf[written - 2] = 0;
 		mlx5_core_info(dev, "lag map active ports: %s\n", buf);
 	} else {
+		#ifndef HAVE_STD_GNU_99
+			int tmp;
+		#endif
 		mlx5_ldev_for_each(i, 0, ldev) {
 			for (j  = 0; j < ldev->buckets; j++) {
 				idx = i * ldev->buckets + j;
@@ -247,10 +257,17 @@ static void mlx5_ldev_free(struct kref *ref)
 	struct mlx5_lag *ldev = container_of(ref, struct mlx5_lag, ref);
 	struct net *net;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (ldev->nb.notifier_call) {
 		net = read_pnet(&ldev->net);
+#ifdef HAVE_UNREGISTER_NETDEVICE_NOTIFIER_NET
 		unregister_netdevice_notifier_net(net, &ldev->nb);
+#else
+		unregister_netdevice_notifier(&ldev->nb);
+#endif
 	}
 
 	mlx5_ldev_for_each(i, 0, ldev) {
@@ -302,7 +319,11 @@ static struct mlx5_lag *mlx5_lag_dev_alloc(struct mlx5_core_dev *dev)
 
 	ldev->nb.notifier_call = mlx5_lag_netdev_event;
 	write_pnet(&ldev->net, mlx5_core_net(dev));
+#ifdef HAVE_UNREGISTER_NETDEVICE_NOTIFIER_NET
 	if (register_netdevice_notifier_net(read_pnet(&ldev->net), &ldev->nb)) {
+#else
+	if (register_netdevice_notifier(&ldev->nb)) {
+#endif
 		ldev->nb.notifier_call = NULL;
 		mlx5_core_err(dev, "Failed to register LAG netdev notifier\n");
 	}
@@ -323,6 +344,9 @@ int mlx5_lag_dev_get_netdev_idx(struct mlx5_lag *ldev,
 				struct net_device *ndev)
 {
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	mlx5_ldev_for_each(i, 0, ldev)
 		if (ldev->pf[i].netdev == ndev)
@@ -334,6 +358,9 @@ int mlx5_lag_dev_get_netdev_idx(struct mlx5_lag *ldev,
 int mlx5_lag_get_dev_index_by_seq(struct mlx5_lag *ldev, int seq)
 {
 	int i, num = 0;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (!ldev)
 		return -ENOENT;
@@ -349,6 +376,9 @@ int mlx5_lag_get_dev_index_by_seq(struct mlx5_lag *ldev, int seq)
 int mlx5_lag_num_devs(struct mlx5_lag *ldev)
 {
 	int i, num = 0;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (!ldev)
 		return 0;
@@ -363,6 +393,9 @@ int mlx5_lag_num_devs(struct mlx5_lag *ldev)
 int mlx5_lag_num_netdevs(struct mlx5_lag *ldev)
 {
 	int i, num = 0;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (!ldev)
 		return 0;
@@ -402,6 +435,9 @@ static void mlx5_infer_tx_affinity_mapping(struct lag_tracker *tracker,
 	u32 rand;
 	int i;
 	int j;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	mlx5_ldev_for_each(i, 0, ldev) {
 		if (tracker->netdev_state[i].tx_enabled &&
@@ -438,6 +474,9 @@ static void mlx5_infer_tx_affinity_mapping(struct lag_tracker *tracker,
 static bool mlx5_lag_has_drop_rule(struct mlx5_lag *ldev)
 {
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	mlx5_ldev_for_each(i, 0, ldev)
 		if (ldev->pf[i].has_drop)
@@ -448,6 +487,9 @@ static bool mlx5_lag_has_drop_rule(struct mlx5_lag *ldev)
 static void mlx5_lag_drop_rule_cleanup(struct mlx5_lag *ldev)
 {
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	mlx5_ldev_for_each(i, 0, ldev) {
 		if (!ldev->pf[i].has_drop)
@@ -537,6 +579,9 @@ static struct net_device *mlx5_lag_active_backup_get_netdev(struct mlx5_core_dev
 	struct mlx5_lag *ldev;
 	unsigned long flags;
 	int i, last_idx;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	spin_lock_irqsave(&lag_lock, flags);
 	ldev = mlx5_lag_dev(dev);
@@ -572,6 +617,9 @@ void mlx5_modify_lag(struct mlx5_lag *ldev,
 	int err;
 	int i;
 	int j;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (first_idx < 0)
 		return;
@@ -731,6 +779,9 @@ static int mlx5_lag_create_single_fdb(struct mlx5_lag *ldev)
 	struct mlx5_core_dev *dev0;
 	int i, j;
 	int err;
+#ifndef HAVE_STD_GNU_99
+	int tmp, tmp1;
+#endif
 
 	if (first_idx < 0)
 		return -EINVAL;
@@ -866,6 +917,9 @@ int mlx5_deactivate_lag(struct mlx5_lag *ldev)
 	struct mlx5_core_dev *dev0;
 	int err;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (first_idx < 0)
 		return -EINVAL;
@@ -916,6 +970,9 @@ bool mlx5_lag_check_prereq(struct mlx5_lag *ldev)
 #endif
 	bool roce_support;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (first_idx < 0 || mlx5_lag_num_devs(ldev) != ldev->ports)
 		return false;
@@ -949,6 +1006,9 @@ bool mlx5_lag_check_prereq(struct mlx5_lag *ldev)
 void mlx5_lag_add_devices(struct mlx5_lag *ldev)
 {
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	mlx5_ldev_for_each(i, 0, ldev) {
 		if (ldev->pf[i].dev->priv.flags &
@@ -963,6 +1023,9 @@ void mlx5_lag_add_devices(struct mlx5_lag *ldev)
 void mlx5_lag_remove_devices(struct mlx5_lag *ldev)
 {
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	mlx5_ldev_for_each(i, 0, ldev) {
 		if (ldev->pf[i].dev->priv.flags &
@@ -982,6 +1045,9 @@ void mlx5_disable_lag(struct mlx5_lag *ldev)
 	bool roce_lag;
 	int err;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (idx < 0)
 		return;
@@ -1018,6 +1084,9 @@ bool mlx5_lag_shared_fdb_supported(struct mlx5_lag *ldev)
 	int idx = mlx5_lag_get_dev_index_by_seq(ldev, MLX5_LAG_P1);
 	struct mlx5_core_dev *dev;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (idx < 0)
 		return false;
@@ -1049,6 +1118,9 @@ static bool mlx5_lag_is_roce_lag(struct mlx5_lag *ldev)
 {
 	bool roce_lag = true;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	mlx5_ldev_for_each(i, 0, ldev)
 		roce_lag = roce_lag && !mlx5_sriov_is_enabled(ldev->pf[i].dev);
@@ -1082,6 +1154,9 @@ mlx5_lag_sum_devices_speed(struct mlx5_lag *ldev, u32 *sum_speed,
 	int pf_idx;
 	u32 speed;
 	int ret;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	*sum_speed = 0;
 	mlx5_ldev_for_each(pf_idx, 0, ldev) {
@@ -1153,6 +1228,9 @@ void mlx5_lag_set_vports_agg_speed(struct mlx5_lag *ldev)
 	struct mlx5_core_dev *mdev;
 	u32 speed;
 	int pf_idx;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (ldev->mode == MLX5_LAG_MODE_MPESW) {
 		if (mlx5_lag_sum_devices_oper_speed(ldev, &speed))
@@ -1184,6 +1262,9 @@ void mlx5_lag_reset_vports_speed(struct mlx5_lag *ldev)
 	u32 speed;
 	int pf_idx;
 	int ret;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	mlx5_ldev_for_each(pf_idx, 0, ldev) {
 		mdev = ldev->pf[pf_idx].dev;
@@ -1213,6 +1294,9 @@ static void mlx5_do_bond(struct mlx5_lag *ldev)
 	bool do_bond, roce_lag;
 	int err;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (idx < 0)
 		return;
@@ -1264,6 +1348,7 @@ static void mlx5_do_bond(struct mlx5_lag *ldev)
 			dev0->priv.flags &= ~MLX5_PRIV_FLAGS_DISABLE_IB_ADEV;
 			mlx5_rescan_drivers_locked(dev0);
 
+#ifdef CONFIG_MLX5_ESWITCH
 			mlx5_ldev_for_each(i, 0, ldev) {
 				err = mlx5_eswitch_reload_ib_reps(ldev->pf[i].dev->priv.eswitch);
 				if (err)
@@ -1280,6 +1365,7 @@ static void mlx5_do_bond(struct mlx5_lag *ldev)
 				mlx5_core_err(dev0, "Failed to enable lag\n");
 				return;
 			}
+#endif
 		}
 		if (tracker.tx_type == NETDEV_LAG_TX_TYPE_ACTIVEBACKUP) {
 			ndev = mlx5_lag_active_backup_get_netdev(dev0);
@@ -1367,6 +1453,9 @@ static int mlx5_handle_changeupper_event(struct mlx5_lag *ldev,
 	int num_slaves = 0;
 	int changed = 0;
 	int i, idx = -1;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	if (!netif_is_lag_master(upper))
 		return 0;
@@ -1632,6 +1721,9 @@ static void mlx5_ldev_remove_netdev(struct mlx5_lag *ldev,
 {
 	unsigned long flags;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	spin_lock_irqsave(&lag_lock, flags);
 	mlx5_ldev_for_each(i, 0, ldev) {
@@ -1976,6 +2068,9 @@ u8 mlx5_lag_get_slave_port(struct mlx5_core_dev *dev,
 	unsigned long flags;
 	u8 port = 0;
 	int i;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	spin_lock_irqsave(&lag_lock, flags);
 	ldev = mlx5_lag_dev(dev);
@@ -2015,6 +2110,9 @@ struct mlx5_core_dev *mlx5_lag_get_next_peer_mdev(struct mlx5_core_dev *dev, int
 	struct mlx5_lag *ldev;
 	unsigned long flags;
 	int idx;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	spin_lock_irqsave(&lag_lock, flags);
 	ldev = mlx5_lag_dev(dev);
@@ -2053,6 +2151,9 @@ int mlx5_lag_query_cong_counters(struct mlx5_core_dev *dev,
 	unsigned long flags;
 	int num_ports;
 	void *out;
+#ifndef HAVE_STD_GNU_99
+	int tmp;
+#endif
 
 	out = kvzalloc(outlen, GFP_KERNEL);
 	if (!out)
